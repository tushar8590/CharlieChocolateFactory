UPDATE msp_electronics SET website = SUBSTRING_INDEX(url,'=',-1) 
ALTER TABLE msp_electronics ADD resolved_url TEXT;


-- add the sql files to run for resolved url cleasnsing and adding the new affiliate id in the end